{
  "name": "rom-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ bitbeckers/rom-goerli",
    "create-local": "graph create --node http://localhost:8020/ bitbeckers/rom-goerli",
    "remove-local": "graph remove --node http://localhost:8020/ bitbeckers/rom-goerli",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bitbeckers/rom-goerli",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.49.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "matchstick-as": "^0.5.2"
  }
}
